import { createComments } from '../src/Comments.js';

describe('Comments', () => {

    describe('fetchComments', () => {
        it('', () => { });
    });











    describe('createComments', () => {
        it('', () => { createComments() });
    });
});
